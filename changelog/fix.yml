added:
  - NPM package `@jelmore1674/github-action-helpers`.
  - "`tryCatch` util to have better error handling."

changed:
  - Restructured some of the action utils during the refactor to implement helper package.

fixed:
  - Issue where the repo was hard coded for issue linking.

removed:
  - "`commitWithApi` helper function and replace with `commit` from helpers package."
  - "`validateInput` helper function and replace with `getValidStringInput` from helpers package."
